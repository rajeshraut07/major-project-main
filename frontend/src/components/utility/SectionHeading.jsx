import React from 'react'

function SectionHeading({ heading }) {
    return (
        <h1 className='font-Cormorant text-bprimary text-7xl font-bold text-center mb-8'>{heading}</h1>
    )
}

export default SectionHeading